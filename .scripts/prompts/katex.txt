请将以下 Markdown 内容中的**数学公式部分**修改为 KaTeX 可正确渲染的格式，并遵守以下规则：

1. **只允许修改数学公式本身**，不得改动任何非数学内容（包括文字、标点、换行、代码块等）。
2. 行内公式统一使用 `\(...\)` 包裹。
3. 独立成行的公式统一使用 `\[...\]` 包裹。
4. **严禁使用 `$$...$$` 和 `$$` 格式的数学公式**。
5. 不允许改变公式的数学语义，只允许修复语法使其适配 KaTeX。
6. 不得新增、删除、打乱原有正文结构。

请直接输出修改后的完整 Markdown，不要添加任何解释说明。
